-- Generated by lampe

import «fraction-0.0.0».Extracted.GeneratedTypes
import Lampe

open Lampe

noir_global_def «fraction-0.0.0»::MAX: «fraction-0.0.0»::Fraction<> = (#_makeData returning «fraction-0.0.0»::Fraction<>)(#_true, (3050913689: u32), (1: u32));

noir_global_def «fraction-0.0.0»::MIN: «fraction-0.0.0»::Fraction<> = (#_makeData returning «fraction-0.0.0»::Fraction<>)(#_false, (3050913689: u32), (1: u32));

noir_global_def «fraction-0.0.0»::ZERO: «fraction-0.0.0»::Fraction<> = (#_makeData returning «fraction-0.0.0»::Fraction<>)(#_true, (0: u32), (1: u32));

noir_global_def «fraction-0.0.0»::ONE: «fraction-0.0.0»::Fraction<> = (#_makeData returning «fraction-0.0.0»::Fraction<>)(#_true, (1: u32), (1: u32));

noir_global_def «fraction-0.0.0»::NEGATIVE_ONE: «fraction-0.0.0»::Fraction<> = (#_makeData returning «fraction-0.0.0»::Fraction<>)(#_false, (1: u32), (1: u32));

noir_def «fraction-0.0.0»::toFraction<>(s: bool, n: u32, d: u32) -> «fraction-0.0.0»::Fraction<> := {
  (#_assert returning Unit)((#_uNeq returning bool)(d, (0: u32)));
  let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(s, n, d);
  fr
}

noir_def «fraction-0.0.0»::invertFraction<>(f: «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<> := {
  (#_assert returning Unit)((#_uNeq returning bool)(f.1, (0: u32)));
  let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f.0, f.2, f.1);
  fr
}

noir_def «fraction-0.0.0»::signChangeFraction<>(f: «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<> := {
  let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)((#_bNot returning bool)(f.0), f.1, f.2);
  fr
}

noir_def «fraction-0.0.0»::multiplyFraction<>(f1: «fraction-0.0.0»::Fraction<>, f2: «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<> := {
  let mut an = (#_cast returning u128)(f1.1);
  let mut ad = (#_cast returning u128)(f1.2);
  let mut bn = (#_cast returning u128)(f2.1);
  let mut bd = (#_cast returning u128)(f2.2);
  let mut m = f1;
  let mut n = f2;
  if (#_bOr returning bool)((#_uGt returning bool)((#_uMul returning u128)(an, bn), (#_cast returning u128)((2000000000: Field))), (#_uGt returning bool)((#_uMul returning u128)(ad, bd), (#_cast returning u128)((2000000000: Field)))) then {
    m = («fraction-0.0.0»::reduceFraction<> as λ(«fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(m);
    n = («fraction-0.0.0»::reduceFraction<> as λ(«fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(n);
    #_skip
  };
  an = (#_cast returning u128)(m.1);
  ad = (#_cast returning u128)(m.2);
  bn = (#_cast returning u128)(n.1);
  bd = (#_cast returning u128)(n.2);
  if (#_bOr returning bool)((#_uGt returning bool)((#_uMul returning u128)(an, bn), (#_cast returning u128)((2000000000: Field))), (#_uGt returning bool)((#_uMul returning u128)(ad, bd), (#_cast returning u128)((2000000000: Field)))) then {
    let mut c = (#_makeData returning «fraction-0.0.0»::Fraction<>)(m.0, m.1, n.2);
    let mut d = (#_makeData returning «fraction-0.0.0»::Fraction<>)(n.0, n.1, m.2);
    c = («fraction-0.0.0»::reduceFraction<> as λ(«fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(c);
    d = («fraction-0.0.0»::reduceFraction<> as λ(«fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(d);
    an = (#_cast returning u128)(c.1);
    ad = (#_cast returning u128)(c.2);
    bn = (#_cast returning u128)(d.1);
    bd = (#_cast returning u128)(d.2);
    #_skip
  };
  let mut ddd = (#_uDiv returning u128)((#_uMul returning u128)(an, bn), (#_uMul returning u128)(ad, bd));
  let mut factor = (1: u128);
  for i in (1: u32) .. (5: u32) do {
    if (#_uLt returning bool)((#_uMul returning u128)(ddd, (#_cast returning u128)((10: Field))), (#_cast returning u128)((2000000000: Field))) then {
      factor = (#_uMul returning u128)(factor, (#_cast returning u128)((10: Field)));
      ddd = (#_uMul returning u128)(ddd, (#_cast returning u128)((10: Field)));
      #_skip
    }
  };
  if (#_bOr returning bool)((#_uGt returning bool)((#_uMul returning u128)(an, bn), (#_cast returning u128)((2000000000: Field))), (#_uGt returning bool)((#_uMul returning u128)(ad, bd), (#_cast returning u128)((2000000000: Field)))) then {
    let fx = (#_cast returning u32)(factor);
    let np = (#_cast returning u32)((#_uDiv returning u128)((#_uMul returning u128)((#_uMul returning u128)(an, bn), factor), (#_uMul returning u128)(ad, bd)));
    let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)((#_bEq returning bool)(f1.0, f2.0), np, fx);
    fr
  } else {
    let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)((#_bEq returning bool)(m.0, n.0), (#_uMul returning u32)(m.1, n.1), (#_uMul returning u32)(m.2, n.2));
    fr
  }
}

noir_def «fraction-0.0.0»::divideFraction<>(f1: «fraction-0.0.0»::Fraction<>, f2: «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<> := {
  (#_assert returning Unit)((#_uNeq returning bool)(f2.1, (0: u32)));
  let f3 = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f2.0, f2.2, f2.1);
  let fr = («fraction-0.0.0»::multiplyFraction<> as λ(«fraction-0.0.0»::Fraction<>, «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(f1, f3);
  fr
}

noir_def «fraction-0.0.0»::reduceFraction<>(f: «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<> := {
  let mut a = f.1;
  let mut b = f.2;
  let mut min = (0: u32);
  let mut j = (1: u32);
  let mut gcd = (1: u32);
  if (#_uGt returning bool)(a, b) then {
    min = b;
    #_skip
  } else {
    min = a;
    #_skip
  };
  for i in (2: u32) .. (100: u32) do {
    j = (#_cast returning u32)(i);
    if (#_uLeq returning bool)(j, min) then {
      if (#_bAnd returning bool)((#_uEq returning bool)((#_uRem returning u32)(a, j), (0: u32)), (#_uEq returning bool)((#_uRem returning u32)(b, j), (0: u32))) then {
        gcd = j;
        #_skip
      }
    }
  };
  let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f.0, (#_uDiv returning u32)(f.1, gcd), (#_uDiv returning u32)(f.2, gcd));
  fr
}

noir_def «fraction-0.0.0»::addFraction<>(f1: «fraction-0.0.0»::Fraction<>, f2: «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<> := {
  let mut an = (#_cast returning u128)(f1.1);
  let mut ad = (#_cast returning u128)(f1.2);
  let mut bn = (#_cast returning u128)(f2.1);
  let mut bd = (#_cast returning u128)(f2.2);
  let mut m = f1;
  let mut n = f2;
  if (#_bEq returning bool)(f1.0, f2.0) then {
    if (#_bOr returning bool)((#_bOr returning bool)((#_bOr returning bool)((#_uGt returning bool)((#_uMul returning u128)(ad, bd), (#_cast returning u128)((2000000000: Field))), (#_uGt returning bool)((#_uAdd returning u128)((#_uMul returning u128)(an, bd), (#_uMul returning u128)(ad, bn)), (#_cast returning u128)((2000000000: Field)))), (#_uGt returning bool)((#_uMul returning u128)(an, bd), (#_cast returning u128)((2000000000: Field)))), (#_uGt returning bool)((#_uMul returning u128)(ad, bn), (#_cast returning u128)((2000000000: Field)))) then {
      m = («fraction-0.0.0»::reduceFraction<> as λ(«fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(m);
      n = («fraction-0.0.0»::reduceFraction<> as λ(«fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(n);
      #_skip
    };
    an = (#_cast returning u128)(m.1);
    ad = (#_cast returning u128)(m.2);
    bn = (#_cast returning u128)(n.1);
    bd = (#_cast returning u128)(n.2);
    if (#_bOr returning bool)((#_bOr returning bool)((#_bOr returning bool)((#_uGt returning bool)((#_uMul returning u128)(ad, bd), (#_cast returning u128)((2000000000: Field))), (#_uGt returning bool)((#_uAdd returning u128)((#_uMul returning u128)(an, bd), (#_uMul returning u128)(ad, bn)), (#_cast returning u128)((2000000000: Field)))), (#_uGt returning bool)((#_uMul returning u128)(an, bd), (#_cast returning u128)((2000000000: Field)))), (#_uGt returning bool)((#_uMul returning u128)(ad, bn), (#_cast returning u128)((2000000000: Field)))) then {
      let mut ddd = (#_uDiv returning u128)((#_uAdd returning u128)((#_uMul returning u128)(an, bd), (#_uMul returning u128)(ad, bn)), (#_uMul returning u128)(ad, bd));
      let mut factor = (1: u128);
      for i in (1: u32) .. (5: u32) do {
        if (#_uLt returning bool)((#_uMul returning u128)(ddd, (#_cast returning u128)((10: Field))), (#_cast returning u128)((2000000000: Field))) then {
          ddd = (#_uMul returning u128)(ddd, (#_cast returning u128)((10: Field)));
          factor = (#_uMul returning u128)(factor, (#_cast returning u128)((10: Field)));
          #_skip
        }
      };
      let np = (#_cast returning u32)((#_uDiv returning u128)((#_uMul returning u128)((#_uAdd returning u128)((#_uMul returning u128)(an, bd), (#_uMul returning u128)(ad, bn)), factor), (#_uMul returning u128)(ad, bd)));
      let fx = (#_cast returning u32)(factor);
      let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f1.0, np, fx);
      fr
    } else {
      let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f1.0, (#_uAdd returning u32)((#_uMul returning u32)(m.1, n.2), (#_uMul returning u32)(n.1, m.2)), (#_uMul returning u32)(m.2, n.2));
      fr
    }
  } else {
    if (#_uGt returning bool)((#_uMul returning u128)(an, bd), (#_uMul returning u128)(bn, ad)) then {
      if (#_bOr returning bool)((#_bOr returning bool)((#_uGt returning bool)((#_uMul returning u128)(ad, bd), (#_cast returning u128)((2000000000: Field))), (#_uGt returning bool)((#_uSub returning u128)((#_uMul returning u128)(an, bd), (#_uMul returning u128)(ad, bn)), (#_cast returning u128)((2000000000: Field)))), (#_uGt returning bool)((#_uMul returning u128)(an, bd), (#_cast returning u128)((2000000000: Field)))) then {
        m = («fraction-0.0.0»::reduceFraction<> as λ(«fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(m);
        n = («fraction-0.0.0»::reduceFraction<> as λ(«fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(n);
        #_skip
      };
      an = (#_cast returning u128)(m.1);
      ad = (#_cast returning u128)(m.2);
      bn = (#_cast returning u128)(n.1);
      bd = (#_cast returning u128)(n.2);
      if (#_bOr returning bool)((#_bOr returning bool)((#_uGt returning bool)((#_uMul returning u128)(ad, bd), (#_cast returning u128)((2000000000: Field))), (#_uGt returning bool)((#_uSub returning u128)((#_uMul returning u128)(an, bd), (#_uMul returning u128)(ad, bn)), (#_cast returning u128)((2000000000: Field)))), (#_uGt returning bool)((#_uMul returning u128)(an, bd), (#_cast returning u128)((2000000000: Field)))) then {
        let mut ddd = (#_uDiv returning u128)((#_uSub returning u128)((#_uMul returning u128)(an, bd), (#_uMul returning u128)(ad, bn)), (#_uMul returning u128)(ad, bd));
        let mut factor = (1: u128);
        for i in (1: u32) .. (5: u32) do {
          if (#_uLt returning bool)((#_uMul returning u128)(ddd, (#_cast returning u128)((10: Field))), (#_cast returning u128)((2000000000: Field))) then {
            ddd = (#_uMul returning u128)(ddd, (#_cast returning u128)((10: Field)));
            factor = (#_uMul returning u128)(factor, (#_cast returning u128)((10: Field)));
            #_skip
          }
        };
        let np = (#_cast returning u32)((#_uDiv returning u128)((#_uMul returning u128)((#_uSub returning u128)((#_uMul returning u128)(an, bd), (#_uMul returning u128)(ad, bn)), factor), (#_uMul returning u128)(ad, bd)));
        let fx = (#_cast returning u32)(factor);
        let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f1.0, np, fx);
        fr
      } else {
        let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f1.0, (#_uSub returning u32)((#_uMul returning u32)(m.1, n.2), (#_uMul returning u32)(n.1, m.2)), (#_uMul returning u32)(m.2, n.2));
        fr
      }
    } else {
      if (#_bOr returning bool)((#_bOr returning bool)((#_uGt returning bool)((#_uMul returning u128)(ad, bd), (#_cast returning u128)((2000000000: Field))), (#_uGt returning bool)((#_uSub returning u128)((#_uMul returning u128)(bn, ad), (#_uMul returning u128)(bd, an)), (#_cast returning u128)((2000000000: Field)))), (#_uGt returning bool)((#_uMul returning u128)(bn, ad), (#_cast returning u128)((2000000000: Field)))) then {
        m = («fraction-0.0.0»::reduceFraction<> as λ(«fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(m);
        n = («fraction-0.0.0»::reduceFraction<> as λ(«fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(n);
        #_skip
      };
      an = (#_cast returning u128)(m.1);
      ad = (#_cast returning u128)(m.2);
      bn = (#_cast returning u128)(n.1);
      bd = (#_cast returning u128)(n.2);
      if (#_bOr returning bool)((#_bOr returning bool)((#_uGt returning bool)((#_uMul returning u128)(ad, bd), (#_cast returning u128)((2000000000: Field))), (#_uGt returning bool)((#_uSub returning u128)((#_uMul returning u128)(bn, ad), (#_uMul returning u128)(bd, an)), (#_cast returning u128)((2000000000: Field)))), (#_uGt returning bool)((#_uMul returning u128)(bn, ad), (#_cast returning u128)((2000000000: Field)))) then {
        let mut ddd = (#_uDiv returning u128)((#_uSub returning u128)((#_uMul returning u128)(bn, ad), (#_uMul returning u128)(bd, an)), (#_uMul returning u128)(ad, bd));
        let mut factor = (1: u128);
        for i in (1: u32) .. (5: u32) do {
          if (#_uLt returning bool)((#_uMul returning u128)(ddd, (#_cast returning u128)((10: Field))), (#_cast returning u128)((2000000000: Field))) then {
            ddd = (#_uMul returning u128)(ddd, (#_cast returning u128)((10: Field)));
            factor = (#_uMul returning u128)(factor, (#_cast returning u128)((10: Field)));
            #_skip
          }
        };
        let np = (#_cast returning u32)((#_uDiv returning u128)((#_uMul returning u128)((#_uSub returning u128)((#_uMul returning u128)(bn, ad), (#_uMul returning u128)(bd, an)), factor), (#_uMul returning u128)(ad, bd)));
        let fx = (#_cast returning u32)(factor);
        let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f2.0, np, fx);
        fr
      } else {
        let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f2.0, (#_uSub returning u32)((#_uMul returning u32)(n.1, m.2), (#_uMul returning u32)(m.1, n.2)), (#_uMul returning u32)(m.2, n.2));
        fr
      }
    }
  }
}

noir_def «fraction-0.0.0»::subtractFraction<>(f1: «fraction-0.0.0»::Fraction<>, f2: «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<> := {
  let f3 = (#_makeData returning «fraction-0.0.0»::Fraction<>)((#_bNot returning bool)(f2.0), f2.1, f2.2);
  let fr = («fraction-0.0.0»::addFraction<> as λ(«fraction-0.0.0»::Fraction<>, «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(f1, f3);
  fr
}

noir_def «fraction-0.0.0»::compareFraction<>(f1: «fraction-0.0.0»::Fraction<>, f2: «fraction-0.0.0»::Fraction<>) -> u32 := {
  if (#_bAnd returning bool)((#_uEq returning bool)(f1.1, f2.1), (#_uEq returning bool)(f1.1, (0: u32))) then {
    (0: u32)
  } else {
    if (#_bNot returning bool)((#_bEq returning bool)(f1.0, f2.0)) then {
      if f1.0 then {
        (1: u32)
      } else {
        (2: u32)
      }
    } else {
      if (#_uGt returning bool)((#_uMul returning u32)(f1.1, f2.2), (#_uMul returning u32)(f2.1, f1.2)) then {
        if f1.0 then {
          (1: u32)
        } else {
          (2: u32)
        }
      } else {
        if (#_bNot returning bool)((#_uEq returning bool)((#_uMul returning u32)(f1.1, f2.2), (#_uMul returning u32)(f2.1, f1.2))) then {
          if f1.0 then {
            (2: u32)
          } else {
            (1: u32)
          }
        } else {
          (0: u32)
        }
      }
    }
  }
}

noir_def «fraction-0.0.0»::squareFraction<>(f: «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<> := {
  let fr = («fraction-0.0.0»::multiplyFraction<> as λ(«fraction-0.0.0»::Fraction<>, «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(f, f);
  fr
}

noir_def «fraction-0.0.0»::cubeFraction<>(f: «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<> := {
  let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f.0, (#_uMul returning u32)((#_uMul returning u32)(f.1, f.1), f.1), (#_uMul returning u32)((#_uMul returning u32)(f.2, f.2), f.2));
  fr
}

noir_def «fraction-0.0.0»::incrFraction<>(f: «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<> := {
  let inc = (#_makeData returning «fraction-0.0.0»::Fraction<>)(#_true, (1: u32), (1: u32));
  let fr = («fraction-0.0.0»::addFraction<> as λ(«fraction-0.0.0»::Fraction<>, «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(f, inc);
  fr
}

noir_def «fraction-0.0.0»::decrFraction<>(f: «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<> := {
  let dec = (#_makeData returning «fraction-0.0.0»::Fraction<>)(#_true, (1: u32), (1: u32));
  let fr = («fraction-0.0.0»::subtractFraction<> as λ(«fraction-0.0.0»::Fraction<>, «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<>)(f, dec);
  fr
}

noir_def «fraction-0.0.0»::isInteger<>(f: «fraction-0.0.0»::Fraction<>) -> bool := {
  if (#_uEq returning bool)(f.1, (0: u32)) then {
    #_true
  } else {
    if (#_uEq returning bool)((#_uRem returning u32)(f.1, f.2), (0: u32)) then {
      #_true
    } else {
      #_false
    }
  }
}

noir_def «fraction-0.0.0»::isNotInteger<>(f: «fraction-0.0.0»::Fraction<>) -> bool := {
  if (#_uEq returning bool)(f.1, (0: u32)) then {
    #_false
  } else {
    if (#_uEq returning bool)((#_uRem returning u32)(f.1, f.2), (0: u32)) then {
      #_false
    } else {
      #_true
    }
  }
}

noir_def «fraction-0.0.0»::floor<>(f: «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<> := {
  let q = (#_uDiv returning u32)(f.1, f.2);
  if (#_uEq returning bool)((#_uMul returning u32)(q, f.2), f.1) then {
    let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f.0, f.1, f.2);
    fr
  } else {
    if f.0 then {
      let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f.0, q, (1: u32));
      fr
    } else {
      let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f.0, (#_uAdd returning u32)(q, (1: u32)), (1: u32));
      fr
    }
  }
}

noir_def «fraction-0.0.0»::ceiling<>(f: «fraction-0.0.0»::Fraction<>) -> «fraction-0.0.0»::Fraction<> := {
  let q = (#_uDiv returning u32)(f.1, f.2);
  if (#_uEq returning bool)((#_uMul returning u32)(q, f.2), f.1) then {
    let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f.0, f.1, f.2);
    fr
  } else {
    if f.0 then {
      let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f.0, (#_uAdd returning u32)(q, (1: u32)), (1: u32));
      fr
    } else {
      let fr = (#_makeData returning «fraction-0.0.0»::Fraction<>)(f.0, q, (1: u32));
      fr
    }
  }
}

def «fraction-0.0.0».Lib.env : Env := Env.mk
  [«fraction-0.0.0::MAX», «fraction-0.0.0::MIN», «fraction-0.0.0::ZERO», «fraction-0.0.0::ONE», «fraction-0.0.0::NEGATIVE_ONE», «fraction-0.0.0::toFraction», «fraction-0.0.0::invertFraction», «fraction-0.0.0::signChangeFraction», «fraction-0.0.0::multiplyFraction», «fraction-0.0.0::divideFraction», «fraction-0.0.0::reduceFraction», «fraction-0.0.0::addFraction», «fraction-0.0.0::subtractFraction», «fraction-0.0.0::compareFraction», «fraction-0.0.0::squareFraction», «fraction-0.0.0::cubeFraction», «fraction-0.0.0::incrFraction», «fraction-0.0.0::decrFraction», «fraction-0.0.0::isInteger», «fraction-0.0.0::isNotInteger», «fraction-0.0.0::floor», «fraction-0.0.0::ceiling»]
  []
