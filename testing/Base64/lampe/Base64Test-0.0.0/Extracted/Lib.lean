-- Generated by lampe

import «Base64Test-0.0.0».Extracted.GeneratedTypes
import Lampe

open Lampe

noir_def «Base64Test-0.0.0»::encode_with_padding<N: u32>(input: Array<u8, N: u32>) -> Array<u8, ((((N * 8) / 6) + (((N % 3) % 2) * 3)) + (((N % 3) / 2) * 2)): u32> := {
  («noir_base64-0.0.0»::encoder::Base64EncodeBE::encode<N: u32> as λ(Array<u8, N: u32>) -> Array<u8, ((((N * 8) / 6) + (((N % 3) % 2) * 3)) + (((N % 3) / 2) * 2)): u32>)(input)
}

noir_def «Base64Test-0.0.0»::encode_no_padding<N: u32>(input: Array<u8, N: u32>) -> Array<u8, ((((N * 8) / 6) + ((N % 3) % 2)) + ((N % 3) / 2)): u32> := {
  («noir_base64-0.0.0»::encoder::Base64EncodeBENoPad::encode<N: u32> as λ(Array<u8, N: u32>) -> Array<u8, ((((N * 8) / 6) + ((N % 3) % 2)) + ((N % 3) / 2)): u32>)(input)
}

def «Base64Test-0.0.0».Lib.env : Env := Env.mk
  [«Base64Test-0.0.0::encode_with_padding», «Base64Test-0.0.0::encode_no_padding»]
  []
