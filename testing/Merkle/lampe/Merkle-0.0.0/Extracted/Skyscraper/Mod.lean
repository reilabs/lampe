-- Generated by lampe

import «Merkle-0.0.0».Extracted.GeneratedTypes
import Lampe

open Lampe

noir_trait_impl[«Merkle-0.0.0».impl_430]<> «Merkle-0.0.0»::hasher::BinaryHasher<Field> for «Merkle-0.0.0»::skyscraper::Skyscraper<> where [] := {
  noir_def hash<>(a: Field, b: Field) -> Field := {
    let x = («Merkle-0.0.0»::permute::permute<> as λ(Array<Field, 2: u32>) -> Array<Field, 2: u32>)((#_mkArray returning Array<Field, 2: u32>)(a, b));
    (#_fAdd returning Field)((#_arrayIndex returning Field)(x, (0: u32)), a)
  };
}

def «Merkle-0.0.0».Skyscraper.Mod.env : Env := Env.mk
  []
  [«Merkle-0.0.0».impl_430]
