-- Generated by lampe

import «noir_sort-0.0.0».Extracted.GeneratedTypes
import Lampe

open Lampe

noir_def «noir_sort-0.0.0»::quicksort::quicksort_explicit::partition<T: Type, N: u32>(arr: & Array<T, N: u32>, low: u32, high: u32, sortfn: λ(T, T) -> bool) -> u32 := {
  (#_fresh returning u32)()
}

noir_def «noir_sort-0.0.0»::quicksort::quicksort_explicit::quicksort_loop<T: Type, N: u32>(arr: & Array<T, N: u32>, low: u32, high: u32, sortfn: λ(T, T) -> bool) -> Unit := {
  (#_fresh returning Unit)()
}

noir_def «noir_sort-0.0.0»::quicksort::quicksort_explicit::quicksort<T: Type, N: u32>(arr: Array<T, N: u32>, sortfn: λ(T, T) -> bool) -> Array<T, N: u32> := {
  (#_fresh returning Array<T, N: u32>)()
}

def «noir_sort-0.0.0».Quicksort.QuicksortExplicit.env : Env := Env.mk
  [«noir_sort-0.0.0::quicksort::quicksort_explicit::partition», «noir_sort-0.0.0::quicksort::quicksort_explicit::quicksort_loop», «noir_sort-0.0.0::quicksort::quicksort_explicit::quicksort»]
  []
