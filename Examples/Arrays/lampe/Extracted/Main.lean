-- Generated by lampe

import Lampe

open Lampe

namespace Extracted

nr_def «mtree_recover»<@N : u32>(idx : [bool; N], p : [Field; N], item : Field) -> Field {
    item;
}

nr_def «main»<>(root : Field, proof : [Field; 32], idx : [bool; 32], item : Field) -> Unit {
    #assert(#fEq(root, (@mtree_recover<32 : u32> as λ([bool; 32], [Field; 32], Field) → Field)(idx, proof, item)) : bool) : Unit;
}


def env := Lampe.Env.mk [«main», «mtree_recover»] []