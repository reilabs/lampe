-- Generated by lampe

import «std-0.0.0».Extracted.GeneratedTypes
import Lampe

open Lampe

namespace «std-0.0.0»
namespace Extracted

nr_def «array::quicksort::partition»<N : U32, T : Type, Env : Type>(arr : &mut<Array<T, N>>, low : u32, high : u32, sortfn : λ(T, T) -> Bool) -> u32 := {
  fresh()
}

nr_def «array::quicksort::quicksort_loop»<T : Type, N : U32, Env : Type>(arr : &mut<Array<T, N>>, low : u32, high : u32, sortfn : λ(T, T) -> Bool) -> Unit := {
  fresh()
}

nr_def «array::quicksort::quicksort»<N : U32, Env : Type, T : Type>(arr : Array<T, N>, sortfn : λ(T, T) -> Bool) -> Array<T, N> := {
  fresh()
}

