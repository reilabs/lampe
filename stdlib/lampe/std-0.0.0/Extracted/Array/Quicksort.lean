-- Generated by lampe

import «std-0.0.0».Extracted.GeneratedTypes
import Lampe

open Lampe

namespace «std-0.0.0»
namespace Extracted

nr_def «array»::«quicksort»::«partition»<T, @N : u32, Env>(arr : & [T; N], low : u32, high : u32, sortfn : λ(T, T) → bool) -> u32 {
    #fresh() : u32
}

nr_def «array»::«quicksort»::«quicksort_loop»<T, @N : u32, Env>(arr : & [T; N], low : u32, high : u32, sortfn : λ(T, T) → bool) -> Unit {
    #fresh() : Unit
}

nr_def «array»::«quicksort»::«quicksort»<T, @N : u32, Env>(arr : [T; N], sortfn : λ(T, T) → bool) -> [T; N] {
    #fresh() : [T; N]
}


def Array.Quicksort.env := Lampe.Env.mk [«array::quicksort::partition», «array::quicksort::quicksort_loop», «array::quicksort::quicksort»] []