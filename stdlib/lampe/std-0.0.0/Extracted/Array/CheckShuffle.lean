-- Generated by lampe

import «std-0.0.0».Extracted.GeneratedTypes
import Lampe

open Lampe

namespace «std-0.0.0»
namespace Extracted

impl «std::cmp::Eq» for «array::check_shuffle::test::CompoundStruct» as impl_23 {
  nr_def eq(self : «array::check_shuffle::test::CompoundStruct», other : «array::check_shuffle::test::CompoundStruct») -> Bool := {
    bAnd(bAnd(bEq(Struct.get(self, 0), Struct.get(other, 0)), fEq(Struct.get(self, 1), Struct.get(other, 1))), uEq(Struct.get(self, 2), Struct.get(other, 2)))
  }
}

nr_def «array::check_shuffle::__get_shuffle_indices»<N : U32, T : Type>(lhs : Array<T, N>, rhs : Array<T, N>) -> Array<u32, N> := {
  fresh()
}

nr_def «array::check_shuffle::__get_index»<N : U32>(indices : Array<u32, N>, idx : u32) -> u32 := {
  fresh()
}

nr_def «array::check_shuffle::check_shuffle»<T : Type, N : U32>(lhs : Array<T, N>, rhs : Array<T, N>) -> Unit := {
  let shuffle_indices : Array<u32, N> = (__get_shuffle_indices<TV_'101, TV_'102> as λ(Array<T, N>, Array<T, N>) -> Array<u32, N>)(lhs, rhs);
  for i in (0 : u32) .. (N : u32) do {
    let idx : u32 = (__get_index<TV_'103> as λ(Array<u32, N>, u32) -> u32)(shuffle_indices, i);
    skip
  }
  skip
}

