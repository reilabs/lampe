-- Generated by lampe

import «std-0.0.0».Extracted.GeneratedTypes
import Lampe

open Lampe

namespace «std-0.0.0»
namespace Extracted

nr_global_def PLO : Field = (53438638232309528389504892708671455233 : Field);

nr_global_def PHI : Field = (64323764613183177041862057485226039389 : Field);

nr_global_def TWO_POW_128 : Field = (340282366920938463463374607431768211456 : Field);

nr_def «field::bn254::compute_decomposition»(x : Field) -> Tuple<Field, Field> := {
  let low : Field = ((x as u128) as Field);
  let high : Field = fDiv(fSub(x, low), TWO_POW_128);
  __tuple.mk(low, high)
}

nr_def «field::bn254::decompose_hint»(x : Field) -> Tuple<Field, Field> := {
  fresh()
}

nr_def «field::bn254::lte_hint»(x : Field, y : Field) -> Bool := {
  fresh()
}

nr_def «field::bn254::assert_gt_limbs»(a : Tuple<Field, Field>, b : Tuple<Field, Field>) -> Unit := {
  let (alo, ahi) : Tuple<Field, Field> = a;
  let (blo, bhi) : Tuple<Field, Field> = b;
  {
    let borrow : Bool = (lte_hint as λ(Field, Field) -> Bool)(alo, blo);
    let rlo : Field = fAdd(fSub(fSub(alo, blo), (1 : Field)), fMul((borrow as Field), TWO_POW_128));
    let rhi : Field = fSub(fSub(ahi, bhi), (borrow as Field));
    (assert_max_bit_size<TV_'309> as λ(Field) -> Unit)(rlo);
    skip
  }
}

nr_def «field::bn254::decompose»(x : Field) -> Tuple<Field, Field> := {
  if (is_unconstrained as λ() -> Bool)() then {
    (compute_decomposition as λ(Field) -> Tuple<Field, Field>)(x)
  } else {
    let (xlo, xhi) : Tuple<Field, Field> = (decompose_hint as λ(Field) -> Tuple<Field, Field>)(x);
    (assert_max_bit_size<TV_'309> as λ(Field) -> Unit)(xlo);
    (assert_max_bit_size<TV_'309> as λ(Field) -> Unit)(xhi);
    assert(fEq(x, fAdd(xlo, fMul(TWO_POW_128, xhi))));
    (assert_gt_limbs as λ(Tuple<Field, Field>, Tuple<Field, Field>) -> Unit)(__tuple.mk(PLO, PHI), __tuple.mk(xlo, xhi));
    __tuple.mk(xlo, xhi)
  }
}

nr_def «field::bn254::assert_gt»(a : Field, b : Field) -> Unit := {
  if (is_unconstrained as λ() -> Bool)() then {
    skip
  } else {
    let a_limbs : Tuple<Field, Field> = (decompose as λ(Field) -> Tuple<Field, Field>)(a);
    let b_limbs : Tuple<Field, Field> = (decompose as λ(Field) -> Tuple<Field, Field>)(b);
    (assert_gt_limbs as λ(Tuple<Field, Field>, Tuple<Field, Field>) -> Unit)(a_limbs, b_limbs)
  }
}

nr_def «field::bn254::assert_lt»(a : Field, b : Field) -> Unit := {
  skip
}

nr_def «field::bn254::gt»(a : Field, b : Field) -> Bool := {
  if (is_unconstrained as λ() -> Bool)() then {
    (field_less_than as λ(Field, Field) -> Bool)(b, a)
  } else if fEq(a, b) then {
    false
  } else {
    if (field_less_than as λ(Field, Field) -> Bool)(a, b) then {
      (assert_gt as λ(Field, Field) -> Unit)(b, a);
      false
    } else {
      (assert_gt as λ(Field, Field) -> Unit)(a, b);
      true
    }
  }
}

nr_def «field::bn254::lt»(a : Field, b : Field) -> Bool := {
  (gt as λ(Field, Field) -> Bool)(b, a)
}

