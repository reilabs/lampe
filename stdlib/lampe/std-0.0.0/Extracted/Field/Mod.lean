-- Generated by lampe

import «std-0.0.0».Extracted.GeneratedTypes
import Lampe

open Lampe

namespace «std-0.0.0»
namespace Extracted

nr_def «field::__assert_max_bit_size»(value : Field, bit_size : u32) -> Unit := {
  apply_range_constraint(value, bit_size)
}

nr_def «field::__to_le_radix»<N : U32>(value : Field, radix : u32) -> Array<u8, N> := {
  to_le_radix(value, radix)
}

nr_def «field::__to_be_radix»<N : U32>(value : Field, radix : u32) -> Array<u8, N> := {
  to_be_radix(value, radix)
}

nr_def «field::__to_le_bits»<N : U32>(value : Field) -> Array<u1, N> := {
  to_le_bits(value)
}

nr_def «field::__to_be_bits»<N : U32>(value : Field) -> Array<u1, N> := {
  to_be_bits(value)
}

nr_def «field::__field_less_than»(x : Field, y : Field) -> Bool := {
  fresh()
}

nr_def «field::field_less_than»(x : Field, y : Field) -> Bool := {
  fresh()
}

nr_def «field::bytes32_to_field»(bytes32 : Array<u8, 32>) -> Field := {
  let mut v : Field = (1 : Field);
  let mut high : Field = ((0 : Field) as Field);
  let mut low : Field = ((0 : Field) as Field);
  for i in (0 : u32) .. (16 : u32) do {
    high = fAdd(high, fMul((arrayIndex(bytes32, (uSub((15 : u32), i) as u32)) as Field), v));
    low = fAdd(low, fMul((arrayIndex(bytes32, (uSub(uAdd((16 : u32), (15 : u32)), i) as u32)) as Field), v));
    skip
  }
  fAdd(low, fMul(high, v))
}

nr_def «field::lt_fallback»(x : Field, y : Field) -> Bool := {
  if (is_unconstrained as λ() -> Bool)() then {
    (field_less_than as λ(Field, Field) -> Bool)(x, y)
  } else {
    let x_bytes : Array<u8, 32> = (to_le_bytes<TV_'312> as λ(Field) -> Array<u8, 32>)(x);
    let y_bytes : Array<u8, 32> = (to_le_bytes<TV_'312> as λ(Field) -> Array<u8, 32>)(y);
    let mut x_is_lt : Bool = false;
    let mut done : Bool = false;
    for i in (0 : u32) .. (32 : u32) do {
      if bNot(done) then {
        let x_byte : u8 = (arrayIndex(x_bytes, (uSub(uSub((32 : u32), (1 : u32)), i) as u32)) as u8);
        let y_byte : u8 = (arrayIndex(y_bytes, (uSub(uSub((32 : u32), (1 : u32)), i) as u32)) as u8);
        let bytes_match : Bool = uEq(x_byte, y_byte);
        if bNot(bytes_match) then {
          x_is_lt = uLt(x_byte, y_byte);
          skip
        }
      }
    }
    x_is_lt
  }
}

