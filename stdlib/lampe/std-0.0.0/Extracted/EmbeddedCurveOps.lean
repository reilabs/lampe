-- Generated by lampe

import «std-0.0.0».Extracted.GeneratedTypes
import Lampe

open Lampe

namespace «std-0.0.0»
namespace Extracted

impl «std::ops::arith::Add» for «embedded_curve_ops::EmbeddedCurvePoint» as impl_25 {
  nr_def add(self : «embedded_curve_ops::EmbeddedCurvePoint», other : «embedded_curve_ops::EmbeddedCurvePoint») -> «embedded_curve_ops::EmbeddedCurvePoint» := {
    (embedded_curve_add as λ(«embedded_curve_ops::EmbeddedCurvePoint», «embedded_curve_ops::EmbeddedCurvePoint») -> «embedded_curve_ops::EmbeddedCurvePoint»)(self, other)
  }
}

impl «std::ops::arith::Sub» for «embedded_curve_ops::EmbeddedCurvePoint» as impl_26 {
  nr_def sub(self : «embedded_curve_ops::EmbeddedCurvePoint», other : «embedded_curve_ops::EmbeddedCurvePoint») -> «embedded_curve_ops::EmbeddedCurvePoint» := {
    («embedded_curve_ops::EmbeddedCurvePoint».add as «embedded_curve_ops::EmbeddedCurvePoint»)(self, («embedded_curve_ops::EmbeddedCurvePoint».neg as λ(«embedded_curve_ops::EmbeddedCurvePoint») -> «embedded_curve_ops::EmbeddedCurvePoint»)(other))
  }
}

impl «std::ops::arith::Neg» for «embedded_curve_ops::EmbeddedCurvePoint» as impl_27 {
  nr_def neg(self : «embedded_curve_ops::EmbeddedCurvePoint») -> «embedded_curve_ops::EmbeddedCurvePoint» := {
    «embedded_curve_ops::EmbeddedCurvePoint».mk(Struct.get(self, 0), fNeg(Struct.get(self, 1)), Struct.get(self, 2))
  }
}

impl «std::cmp::Eq» for «embedded_curve_ops::EmbeddedCurvePoint» as impl_28 {
  nr_def eq(self : «embedded_curve_ops::EmbeddedCurvePoint», b : «embedded_curve_ops::EmbeddedCurvePoint») -> Bool := {
    bOr(bAnd(Struct.get(self, 2), Struct.get(b, 2)), bAnd(bAnd(bEq(Struct.get(self, 2), Struct.get(b, 2)), fEq(Struct.get(self, 0), Struct.get(b, 0))), fEq(Struct.get(self, 1), Struct.get(b, 1))))
  }
}

impl «std::hash::Hash» for «embedded_curve_ops::EmbeddedCurvePoint» as impl_29 {
  nr_def hash<H : Type>(self : «embedded_curve_ops::EmbeddedCurvePoint», state : &mut<H>) -> Unit := {
    if Struct.get(self, 2) then {
      skip
    } else {
      (Field.hash<TV_'321> as λ(Field, &mut<H>) -> Unit)(Struct.get(self, 0), state);
      skip
    }
  }
}

impl «std::cmp::Eq» for «embedded_curve_ops::EmbeddedCurveScalar» as impl_30 {
  nr_def eq(self : «embedded_curve_ops::EmbeddedCurveScalar», other : «embedded_curve_ops::EmbeddedCurveScalar») -> Bool := {
    bAnd(fEq(Struct.get(other, 1), Struct.get(self, 1)), fEq(Struct.get(other, 0), Struct.get(self, 0)))
  }
}

impl «std::hash::Hash» for «embedded_curve_ops::EmbeddedCurveScalar» as impl_31 {
  nr_def hash<H : Type>(self : «embedded_curve_ops::EmbeddedCurveScalar», state : &mut<H>) -> Unit := {
    (Field.hash<TV_'321> as λ(Field, &mut<H>) -> Unit)(Struct.get(self, 1), state);
    skip
  }
}

nr_def «embedded_curve_ops::multi_scalar_mul»<N : U32>(points : Array<«embedded_curve_ops::EmbeddedCurvePoint», N>, scalars : Array<«embedded_curve_ops::EmbeddedCurveScalar», N>) -> «embedded_curve_ops::EmbeddedCurvePoint» := {
  arrayIndex((multi_scalar_mul_array_return<TV_'118> as λ(Array<«embedded_curve_ops::EmbeddedCurvePoint», N>, Array<«embedded_curve_ops::EmbeddedCurveScalar», N>) -> Array<«embedded_curve_ops::EmbeddedCurvePoint», 1>)(points, scalars), ((0 : u32) as u32))
}

nr_def «embedded_curve_ops::multi_scalar_mul_array_return»<N : U32>(points : Array<«embedded_curve_ops::EmbeddedCurvePoint», N>, scalars : Array<«embedded_curve_ops::EmbeddedCurveScalar», N>) -> Array<«embedded_curve_ops::EmbeddedCurvePoint», 1> := {
  multi_scalar_mul(points, scalars)
}

nr_def «embedded_curve_ops::fixed_base_scalar_mul»(scalar : «embedded_curve_ops::EmbeddedCurveScalar») -> «embedded_curve_ops::EmbeddedCurvePoint» := {
  (multi_scalar_mul<TV_'117> as λ(Array<«embedded_curve_ops::EmbeddedCurvePoint», 1>, Array<«embedded_curve_ops::EmbeddedCurveScalar», 1>) -> «embedded_curve_ops::EmbeddedCurvePoint»)(mkArray((««embedded_curve_ops::EmbeddedCurvePoint»::generator» as λ() -> «embedded_curve_ops::EmbeddedCurvePoint»)()), mkArray(scalar))
}

nr_def «embedded_curve_ops::embedded_curve_add»(point1 : «embedded_curve_ops::EmbeddedCurvePoint», point2 : «embedded_curve_ops::EmbeddedCurvePoint») -> «embedded_curve_ops::EmbeddedCurvePoint» := {
  if (is_unconstrained as λ() -> Bool)() then {
    if Struct.get(point1, 2) then {
      point2
    } else if Struct.get(point2, 2) then {
      point1
    } else {
      (embedded_curve_add_unsafe as λ(«embedded_curve_ops::EmbeddedCurvePoint», «embedded_curve_ops::EmbeddedCurvePoint») -> «embedded_curve_ops::EmbeddedCurvePoint»)(point1, point2)
    }
  } else {
    let x_coordinates_match : Bool = fEq(Struct.get(point1, 0), Struct.get(point2, 0));
    let y_coordinates_match : Bool = fEq(Struct.get(point1, 1), Struct.get(point2, 1));
    let double_predicate : Bool = bAnd(x_coordinates_match, y_coordinates_match);
    let infinity_predicate : Bool = bAnd(x_coordinates_match, bNot(y_coordinates_match));
    let point1_1 : «embedded_curve_ops::EmbeddedCurvePoint» = «embedded_curve_ops::EmbeddedCurvePoint».mk(fAdd(Struct.get(point1, 0), (x_coordinates_match as Field)), Struct.get(point1, 1), false);
    let point2_1 : «embedded_curve_ops::EmbeddedCurvePoint» = «embedded_curve_ops::EmbeddedCurvePoint».mk(Struct.get(point2, 0), Struct.get(point2, 1), false);
    let mut result : «embedded_curve_ops::EmbeddedCurvePoint» = (embedded_curve_add_unsafe as λ(«embedded_curve_ops::EmbeddedCurvePoint», «embedded_curve_ops::EmbeddedCurvePoint») -> «embedded_curve_ops::EmbeddedCurvePoint»)(point1_1, point2_1);
    let double : «embedded_curve_ops::EmbeddedCurvePoint» = (embedded_curve_add_unsafe as λ(«embedded_curve_ops::EmbeddedCurvePoint», «embedded_curve_ops::EmbeddedCurvePoint») -> «embedded_curve_ops::EmbeddedCurvePoint»)(point1, point1);
    result = if double_predicate then {
      double
    } else {
      result
    };
    if Struct.get(point1, 2) then {
      skip
    };
    if Struct.get(point2, 2) then {
      skip
    };
    let mut result_is_infinity : Bool = bAnd(infinity_predicate, bAnd(bNot(Struct.get(point1, 2)), bNot(Struct.get(point2, 2))));
    (Object.at(result, 2) : Bool) = bOr(result_is_infinity, bAnd(Struct.get(point1, 2), Struct.get(point2, 2)));
    result
  }
}

nr_def «embedded_curve_ops::embedded_curve_add_array_return»(_point1 : «embedded_curve_ops::EmbeddedCurvePoint», _point2 : «embedded_curve_ops::EmbeddedCurvePoint») -> Array<«embedded_curve_ops::EmbeddedCurvePoint», 1> := {
  embedded_curve_add(_point1, _point2)
}

nr_def «embedded_curve_ops::embedded_curve_add_not_nul»(point1 : «embedded_curve_ops::EmbeddedCurvePoint», point2 : «embedded_curve_ops::EmbeddedCurvePoint») -> «embedded_curve_ops::EmbeddedCurvePoint» := {
  assert(fNeq(Struct.get(point1, 0), Struct.get(point2, 0)));
  assert(bNot(Struct.get(point1, 2)));
  assert(bNot(Struct.get(point2, 2)));
  let point1_1 : «embedded_curve_ops::EmbeddedCurvePoint» = «embedded_curve_ops::EmbeddedCurvePoint».mk(Struct.get(point1, 0), Struct.get(point1, 1), false);
  let point2_1 : «embedded_curve_ops::EmbeddedCurvePoint» = «embedded_curve_ops::EmbeddedCurvePoint».mk(Struct.get(point2, 0), Struct.get(point2, 1), false);
  (embedded_curve_add_unsafe as λ(«embedded_curve_ops::EmbeddedCurvePoint», «embedded_curve_ops::EmbeddedCurvePoint») -> «embedded_curve_ops::EmbeddedCurvePoint»)(point1_1, point2_1)
}

nr_def «embedded_curve_ops::embedded_curve_add_unsafe»(point1 : «embedded_curve_ops::EmbeddedCurvePoint», point2 : «embedded_curve_ops::EmbeddedCurvePoint») -> «embedded_curve_ops::EmbeddedCurvePoint» := {
  arrayIndex((embedded_curve_add_array_return as λ(«embedded_curve_ops::EmbeddedCurvePoint», «embedded_curve_ops::EmbeddedCurvePoint») -> Array<«embedded_curve_ops::EmbeddedCurvePoint», 1>)(point1, point2), ((0 : u32) as u32))
}

