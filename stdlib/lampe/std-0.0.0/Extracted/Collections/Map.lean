-- Generated by lampe

import «std-0.0.0».Extracted.GeneratedTypes
import Lampe

open Lampe

namespace «std-0.0.0»
namespace Extracted

nr_global_def MAX_LOAD_FACTOR_NUMERATOR : u32 = (3 : u32);

nr_global_def MAX_LOAD_FACTOR_DEN0MINATOR : u32 = (4 : u32);

impl<K : Type, V : Type> «std::default::Default» for «collections::map::Slot»<K, V> as impl_34 {
  nr_def default() -> «collections::map::Slot»<K, V> := {
    «collections::map::Slot».mk<K, V>((««option::Option»::none»<TV_'294> as λ() -> «option::Option»<Tuple<K, V>>)(), false)
  }
}

impl<K : Type, V : Type, N : U32, B : Type, «<B as BuildHasher>::H» : Type> «std::cmp::Eq» for «collections::map::HashMap»<K, V, N, B> as impl_35 {
  nr_def eq(self : «collections::map::HashMap»<K, V, N, B>, other : «collections::map::HashMap»<K, V, N, B>) -> Bool := {
    let mut equal : Bool = false;
    if uEq((««collections::map::HashMap»::len»<TV_'208, TV_'209, TV_'206, TV_'207> as λ(«collections::map::HashMap»<K, V, N, B>) -> u32)(self), (««collections::map::HashMap»::len»<TV_'207, TV_'208, TV_'209, TV_'206> as λ(«collections::map::HashMap»<K, V, N, B>) -> u32)(other)) then {
      equal = true;
      {
        let $i0 : Array<«collections::map::Slot»<K, V>, N> = Struct.get(self, 0);
        skip
      }
    };
    equal
  }
}

impl<K : Type, V : Type, N : U32, B : Type, «<B as BuildHasher>::H» : Type> «std::default::Default» for «collections::map::HashMap»<K, V, N, B> as impl_36 {
  nr_def default() -> «collections::map::HashMap»<K, V, N, B> := {
    let _build_hasher : B = (B.default<TV_'43> as λ() -> B)();
    let map : «collections::map::HashMap»<K, V, N, B> = (««collections::map::HashMap»::with_hasher»<TV_'206, TV_'208, TV_'207, TV_'209, TV_'210> as λ(B) -> «collections::map::HashMap»<K, V, N, B>)(_build_hasher);
    map
  }
}

