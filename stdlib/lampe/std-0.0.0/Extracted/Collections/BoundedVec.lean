-- Generated by lampe

import «std-0.0.0».Extracted.GeneratedTypes
import Lampe

open Lampe

namespace «std-0.0.0»
namespace Extracted

impl<T : Type, MaxLen : U32> «std::cmp::Eq» for «collections::bounded_vec::BoundedVec»<T, MaxLen> as impl_32 {
  nr_def eq(self : «collections::bounded_vec::BoundedVec»<T, MaxLen>, other : «collections::bounded_vec::BoundedVec»<T, MaxLen>) -> Bool := {
    if uEq(Struct.get(self, 1), Struct.get(other, 1)) then {
      (Array<T, MaxLen>.eq as Bool)(Struct.get(self, 0), Struct.get(other, 0))
    } else {
      false
    }
  }
}

impl<Len : U32, T : Type, MaxLen : U32> «std::convert::From»<Array<T, Len>> for «collections::bounded_vec::BoundedVec»<T, MaxLen> as impl_33 {
  nr_def from(array : Array<T, Len>) -> «collections::bounded_vec::BoundedVec»<T, MaxLen> := {
    (««collections::bounded_vec::BoundedVec»::from_array»<TV_'244, TV_'241, TV_'240> as λ(Array<T, Len>) -> «collections::bounded_vec::BoundedVec»<T, MaxLen>)(array)
  }
}

