-- Generated by lampe

import «std-0.0.0».Extracted.GeneratedTypes
import Lampe

open Lampe

namespace «std-0.0.0»
namespace Extracted

nr_def print_oracle<T : Type>(with_newline : Bool, input : T) -> Unit := {
  fresh()
}

nr_def print_unconstrained<T : Type>(with_newline : Bool, input : T) -> Unit := {
  fresh()
}

nr_def println<T : Type>(input : T) -> Unit := {
  skip
}

nr_def print<T : Type>(input : T) -> Unit := {
  skip
}

nr_def verify_proof<N : U32, M : U32, K : U32>(verification_key : Array<Field, N>, proof : Array<Field, M>, public_inputs : Array<Field, K>, key_hash : Field) -> Unit := {
  skip
}

nr_def verify_proof_with_type<N : U32, M : U32, K : U32>(verification_key : Array<Field, N>, proof : Array<Field, M>, public_inputs : Array<Field, K>, key_hash : Field, proof_type : u32) -> Unit := {
  if bNot((is_unconstrained as λ() -> Bool)()) then {
    skip
  };
  skip
}

nr_def verify_proof_internal<M : U32, K : U32, N : U32>(verification_key : Array<Field, N>, proof : Array<Field, M>, public_inputs : Array<Field, K>, key_hash : Field, proof_type : u32) -> Unit := {
  recursive_aggregation(verification_key, proof, public_inputs, key_hash, proof_type)
}

nr_def assert_constant<T : Type>(x : T) -> Unit := {
  assert_constant(x)
}

nr_def static_assert<T : Type>(predicate : Bool, message : T) -> Unit := {
  static_assert(predicate, message)
}

nr_def wrapping_add<T : Type>(x : T, y : T) -> T := {
  (Field<T>.as_<TV_'23, TV_'24> as λ(Field) -> T)(fAdd((T<Field>.as_<TV_'23, TV_'24> as λ(T) -> Field)(x), (T<Field>.as_<TV_'23, TV_'24> as λ(T) -> Field)(y)))
}

nr_def wrapping_sub<T : Type>(x : T, y : T) -> T := {
  (Field<T>.as_<TV_'24, TV_'23> as λ(Field) -> T)(fSub(fAdd((T<Field>.as_<TV_'23, TV_'24> as λ(T) -> Field)(x), (340282366920938463463374607431768211456 : Field)), (T<Field>.as_<TV_'24, TV_'23> as λ(T) -> Field)(y)))
}

nr_def wrapping_mul<T : Type>(x : T, y : T) -> T := {
  (Field<T>.as_<TV_'24, TV_'23> as λ(Field) -> T)(fMul((T<Field>.as_<TV_'24, TV_'23> as λ(T) -> Field)(x), (T<Field>.as_<TV_'23, TV_'24> as λ(T) -> Field)(y)))
}

nr_def as_witness(x : Field) -> Unit := {
  as_witness(x)
}

